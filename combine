Manual Count = 
CALCULATE(
    DISTINCTCOUNT('Manual'[Associated Release]),
    ALLEXCEPT('Manual', 'Manual'[Weekly])
)

Automation Count = 
CALCULATE(
    DISTINCTCOUNT('Automation'[Associated Release]),
    ALLEXCEPT('Automation', 'Automation'[Weekly])
)
