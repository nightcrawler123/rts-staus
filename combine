Manual Count = 
CALCULATE(
    DISTINCTCOUNT('Manual'[Associated Release]),
    FILTER('Manual', 'Manual'[Weekly] = EARLIER('Manual'[Weekly]))
)

Automation Count = 
CALCULATE(
    DISTINCTCOUNT('Automation'[Associated Release]),
    FILTER('Automation', 'Automation'[Weekly] = EARLIER('Automation'[Weekly]))
)
